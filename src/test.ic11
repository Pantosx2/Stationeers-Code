void doNothing(){}

void Main()
{
    var array = {1, 2, 3}; // The length of the array doesn't matter

    while(true)
    {
        if(false) // The condition doesn't matter 
        {
            doNothing();
        }
        else
        {
            array[2] = 42;
        }
    }
}